# Sample 1 — photographic, cream & forest

First design pass for Rise & Thrive Support Services. Warm editorial look: cream paper, forest green, Fraunces, and generated photography of a home.

Kept as a reference. The live brief and newer gold logos live at the repo root; **sample2** is the current direction.

```bash
cd sample1
cp .env.example .env.local   # already present
npm install
npm run dev
```
